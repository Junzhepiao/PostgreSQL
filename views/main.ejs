<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <title>TRANSACTIONS API</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css"
      integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS"
      crossorigin="anonymous"
    />
  </head>
  <body >
    <div class="container">
      <div class="row"> 
        <div class="col-sm-12">
    <h2> Transactions</h2>

    <% for(let i = 0; i<transactions.length; i++){ %>
      <form action="/updateTrans/<%=transactions[i].id%>" method="POST">
        ID:<input type="integer" name="id" maxlength="4" size="4" value="<%=transactions[i].id%>">
        Amount:<input type="integer" maxlength="10" size="10" name="amount" value="<%=transactions[i].amount%>">
        Type:<input type="text" name="type" value="<%=transactions[i].type%>">
        Business Name<input type="text" name="business_name" value="<%=transactions[i].business_name%>">
        <button type="submit" class="btn btn-primary">Update</button>
      </form>
        <a class="btn btn-primary btn-sm" href="/deleteTrans/<%= transactions[i].id %>"> Delete</a>
    <% } %>
    </div>
    </div>
    </div>
    <hr>

    <div class="container">
    <h2>Post a transaction</h2>
    <form action="/postTrans" method="POST">
      <select name='user_id'>
      <% for ( let i = 0; i < users.length; i++) { %>
          <option value='<%=users[i].id%>'><%=users[i].email%></option>
      <% } %>
      </select>
      <input type="integer" name="amount" placeholder="amount">
      <input type="text" name="type" placeholder="type">
      <input type="text" name="business_name" placeholder="Business Name">

      <button type="submit" class="btn btn-primary">Submit</button>
    </form>
    </div>
    <hr>



  </body>
</html>